# Runtime
google-cloud-storage>=2.16,<3
google-cloud-bigquery>=3.20,<4
google-genai>=1.0
PyYAML>=6.0
python-dotenv>=1.0

# Tests / tooling (acceso al cloud en solo lectura)
# truststore usa el almacen de certificados del SO -> TLS al cloud tras proxy corporativo.
pytest>=8.0
truststore>=0.9
